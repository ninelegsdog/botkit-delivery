# Required env vars for botkit-delivery (deploy uses --env-file on VPS)
# Copy to /home/algtro/BOTOGRAD_TECH/botkit-delivery/.env and fill secrets.

# WEBHOOK bot: after deploy run `set_webhook` with the secret below set.
#   bookingbot/membership -> BOTKIT__WEBHOOK_SECRET_TOKEN
#   delivery             -> WEBHOOK_SECRET

ADMIN_IDS=# REQUIRED
ADMIN_PASSWORD=# REQUIRED
BOT_TOKEN=# REQUIRED
DB_PATH=
LOG_LEVEL=
METRICS_PORT=
REDIS_URL=
SENTRY_DSN=
WEBHOOK_SECRET=
